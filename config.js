/* Public settings. Never put permanent TURN credentials or private keys here.
   peer: {host:'signal.example.com',port:443,path:'/peerjs',secure:true}
   iceEndpoint: same-origin HTTPS endpoint issuing short-lived RTCIceServer entries. */
window.PGH_CONFIG={version:'1.0.0-rc.1.2',jungle:{roundSeconds:50},peer:{},iceEndpoint:null,hubUrl:null};
